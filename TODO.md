# TODO

**Work is tracked as GitHub issues.** See
[open issues](https://github.com/skomp/tutorAIl/issues), labelled `created-by-claude`
where Claude filed them.

This file records only what an issue cannot: a decision that is settled and must not be
re-litigated, or a fact that was measured and should not be measured again. Both of those
belong in the design specification when they concern the format or the runner, so this
file is usually empty.

Deferred design work lives in
`docs/superpowers/specs/2026-09-11-tutorial-runner-design.md`, sections 15a to 15d
(settled decisions) and 16 (telemetry, deliberately not built).
