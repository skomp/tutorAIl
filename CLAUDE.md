# tutorAIl

## Task tracking

This project records deferred work in `TODO.md`. It does not use GitHub issues, and the
repository has no issue, open or closed.

Write a new item in `TODO.md`. Do not open a GitHub issue for this repository.

## Repositories this project spans

Three repositories work together. Name the repository whenever you refer to a commit, a
branch or a file outside the one you are in.

| Repository | Holds |
|---|---|
| `skomp/tutorAIl` | the runner: the skill, the references, `validate_bundle.py`, `catalogs.py` |
| `skomp/tutorail-bundles` | the courses, and the `catalog.yaml` that lists them |
| `skomp/tutorail-authoring` | the authoring toolkit and the course-quality skill |

Several agent sessions work in these repositories at the same time. Read
`parallel-sessions` before you stage, commit, or create a worktree.
