# Worked example: merging two suffixes

Two words share a suffix. The naive structure stores the suffix twice. The
merged structure stores it once and points both prefixes at it.

    cat, cats  ->  c-a-t-(s?)

Walk the learner through the equivalence test before showing the merge.
