# GENERATED by tutorail-authoring's catalog.py. Edit the bundles'
# tutorial.yaml files and run it again rather than editing this file.
#
# This header is a copy of the real one in skomp/tutorail-bundles, and it is
# the point of the fixture. It NAMES a field in prose:
#
#   `optional_lesson_count` is derived from that bundle's `optional_lessons`
#   mapping and is written even when it is 0. Do not write
#   `optional_lesson_cnt`, which no runner reads.
#
# So this file contains the strings `optional_lesson_count`,
# `optional_lessons` and `optional_lesson_cnt` OUTSIDE any entry. A check
# that searched the text would report a field that is not there, and would
# miscount the field that is. Measured on the real catalogue, which carries
# a header of exactly this shape:
#
#   entries                                          5
#   loose    grep -c optional_lesson_count           6
#   anchored grep -c '^    optional_lesson_count: '  5
#
# Remove the field from one entry and the loose count becomes 5, which
# equals the entry count and reads as complete. The one case such a check
# exists to catch is the one case it passes. Check 8 therefore reads the
# KEYS of the parsed entry and never the file's text.
#
# The second half of the fixture is the other direction: a comment must not
# SUPPRESS a report either, so `durable-event-broker` below carries a real
# unknown key and check 3 must warn about exactly that one.

catalog_version: 1
tutorials:
  - id: rust-cli-basics
    title: Build a Command-Line Tool in Rust
    description: >
      A short course that builds a small command-line tool. This sentence
      mentions optional_lesson_cnt and optional_lesson_count inside a
      description, where neither is a field and neither may be reported.
    subjects: [rust, command-line-tools]
    aliases: [cli, argument-parsing]
    level: beginner
    style: [project-driven]
    scope: "3 lessons; an afternoon"
    optional_lesson_count: 3
    workspace_kind: new-repository
    source:
      type: local
      path: ../rust-cli-basics

  - id: durable-event-broker
    title: Build a Durable Event Broker
    description: >
      Build a single-node event broker with a retained log, topic partitions
      and offsets.
    subjects: [go, event-streaming]
    aliases: [message-broker, event-log]
    level: intermediate
    style: [project-driven, long-form]
    scope: "15 lessons; a few weeks"
    optional_lesson_count: 0
    curriculum_owner: the-platform-team
    workspace_kind: new-repository
    source:
      type: local
      path: ../durable-event-broker
