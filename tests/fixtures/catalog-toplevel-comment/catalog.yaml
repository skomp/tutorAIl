# GENERATED by tutorail-authoring's catalog.py. Edit the bundles'
# tutorial.yaml files and run it again rather than editing this file.
#
# This header is the point of the fixture: it NAMES top-level keys in prose,
# each one followed by a colon, which is exactly the shape a text-scanning
# implementation of check 1 would pick up.
#
# The keys this document defines:
#
#   catalog_version: 1 for this document; an unknown version is an error
#   tutorials: the list of entries
#
# And the keys that were considered and deliberately NOT written, which a
# reader of this header must not mistake for fields:
#
#   catalog_defaults: rejected; an entry says what it needs
#   registry_url: rejected; catalogs.yaml already says where catalogues are
#   refreshed_at: rejected; freshness belongs to the cache, not the file
#
# So this file contains the strings `catalog_defaults:`, `registry_url:` and
# `refreshed_at:` OUTSIDE any key position, plus `registry_url:` again
# inside a description below. A check that searched the text would report
# four top-level fields that are not there.
#
# The other direction matters just as much: a comment must not SUPPRESS a
# report either. `catalog_notes` below is a REAL unknown top-level key, at
# column 0, and check 1 must warn about exactly that one and no other.

catalog_version: 1
catalog_notes: assembled by hand for this fixture
tutorials:
  - id: rust-cli-basics
    title: Build a Command-Line Tool in Rust
    description: >
      A short course that builds a small command-line tool. This sentence
      mentions registry_url: and refreshed_at: inside a description, where
      neither is a key and neither may be reported.
    subjects: [rust, command-line-tools]
    aliases: [cli, argument-parsing]
    level: beginner
    style: [project-driven]
    scope: "3 lessons; an afternoon"
    optional_lesson_count: 3
    workspace_kind: new-repository
    source:
      type: local
      path: ../rust-cli-basics

  - id: durable-event-broker
    title: Build a Durable Event Broker
    description: >
      Build a single-node event broker with a retained log, topic partitions
      and offsets.
    subjects: [go, event-streaming]
    aliases: [message-broker, event-log]
    level: intermediate
    style: [project-driven, long-form]
    scope: "15 lessons; a few weeks"
    optional_lesson_count: 0
    workspace_kind: new-repository
    source:
      type: local
      path: ../durable-event-broker
